## 1. Реализовать функцию, которая найдет сумму массива

```js
const arr: (number | string)[] = [10, 40, 53, '14', 84, 100, 54];

const sum = (arr: (number | string)[]): number => { }
```

## ----

## Реализовать функцию которая найдет сумму массива

```js
const arr: (number | string)[] = [10, 40, 53, '14', 84, 100, 54];

const summ = (arr: (number | string)[]): number => {};
```

## Найти сумму всех значений в дереве

```ts
interface INode {
  value: number;
  left?: INode;
  right?: INode;
}

const tree: INode = {
  value: 5,
  left: {
    value: 10,
    left: {
      value: 82,
      right: {
        value: 30,
      },
    },
    right: {
      value: 68,
    },
  },
  right: {
    value: 21,
    right: {
      value: 62,
    },
  },
};

const sumTree = (tree: INode): number => {};
```
